import { describe, it } from 'vitest'
// import fc, { Arbitrary } from 'fast-check'
// import { stripLeading as sl } from './strings'

describe('stripLeading', () => {
	it('has no leading whitespace', () => {
		// fc.assert(fc.property(stringWithSpecials, ([s, specials]) => {}))
	})
	it('substitutes the values correctly', () => {
		// TODO
	})
	it('does the whole shebang', () => {
		// TODO
	})
})
