import { describe, it } from 'vitest'

describe('stripLeading', () => {
	it('has the same number of new lines', async () => {
		// TODO
	})
	it('has the same number of carriage returns', async () => {
		// TODO
	})
	it('has no leading whitespace', async () => {
		// TODO
	})
	it('substitutes the values correctly', async () => {
		// TODO
	})
})
